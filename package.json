{
  "name": "buku-flow",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --hot src/main.ts",
    "start": "bun run src/main.ts",
    "build": "bun build --minify --outfile dist/main.js --target bun src/main.ts",
    "reminder": "bun run src/reminder.cron.ts"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "hono": "^4.10.7",
    "mysql2": "^3.15.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.10.1"
  }
}